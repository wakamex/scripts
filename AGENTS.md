For substantial Python CLIs with hyphenated command names, keep a thin hyphenated launcher and an
underscore-named importable module so tests can import it normally. Small standalone scripts can
remain one file.
